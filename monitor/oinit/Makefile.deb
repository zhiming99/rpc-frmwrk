all:
	python3 setup.py bdist_wheel

bindir=${PWD}/usr/bin/rpcf
etcdir=${PWD}/etc/rpcf
install:
	if [ ! -d ${bindir} ];then mkdir -p ${bindir};fi
	install -m 0644 dist/oinit-*.whl ${bindir}
	if [ ! -d ${etcdir} ];then mkdir -p ${etcdir};fi
	install -m 0644 oinit/oinitdesc.json ${etcdir}

distclean:
	rm ${bindir}/* || true
	rm ${etcdir}/* || true
