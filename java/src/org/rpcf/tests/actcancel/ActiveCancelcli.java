// GENERATED BY RIDLC. MAKE SURE TO BACKUP BEFORE RE-COMPILING.
package org.rpcf.tests.actcancel;
import org.rpcf.rpcbase.*;
import java.util.Map;
import java.util.HashMap;
import java.lang.String;
import java.nio.ByteBuffer;
import java.util.concurrent.Semaphore;

public class ActiveCancelcli extends ActiveCancelclibase
{
    public Semaphore m_sem = new Semaphore(0);
    public ActiveCancelcli( ObjPtr pIoMgr,
        String strDesc, String strSvrObj )
    { super( pIoMgr, strDesc, strSvrObj ); }
    
    // IActiveCancel::LongWait async callback
    public void onLongWaitComplete(
        Object oContext, int iRet,
        String i0r  )
    {
        // IActiveCancel::LongWait has completed
        if( iRet == RC.ERROR_USER_CANCEL)
            rpcbase.JavaOutputMsg("'LongWait' is canceled");
        else if(RC.SUCCEEDED(iRet))
            rpcbase.JavaOutputMsg(
                    "'LongWait' completed with '" + i0r + "'");
        return;
    }
    
    
}