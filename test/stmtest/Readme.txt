Streaming test

1. test if the stream channel can setup properly between server and proxy.

2. test if the stream data can be transferred correctly between server and
proxy.

3. test if the stream can be closed gracefully.

4. test if the stream can be canceled at proxy side.

5. test if the stream server can shutdown the stream correctly on bad transfer.

6. test if the IO error can be handled properly on both side.
