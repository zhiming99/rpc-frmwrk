// Generated by ridlc
#include "rpc.h"
#include "proxy.h"
using namespace rpcf;
#include "AsyncTestcli.h"

ObjPtr g_pIoMgr;

extern FactoryPtr InitClassFactory();
gint32 InitContext()
{
    gint32 ret = CoInitialize( 0 );
    if( ERROR( ret ) )
        return ret;
    do{
        // load class factory for 'asynctst'
        FactoryPtr p = InitClassFactory();
        ret = CoAddClassFactory( p );
        if( ERROR( ret ) )
            break;
        
        CParamList oParams;
        oParams.Push( "asynctstcli" );

        // adjust the thread number if necessary
        oParams[ propMaxIrpThrd ] = 0;
        oParams[ propMaxTaskThrd ] = 1;
        
        ret = g_pIoMgr.NewObj(
            clsid( CIoManager ), 
            oParams.GetCfg() );
        if( ERROR( ret ) )
            break;
        
        IService* pSvc = g_pIoMgr;
        ret = pSvc->Start();
        
    }while( 0 );

    if( ERROR( ret ) )
    {
        g_pIoMgr.Clear();
        CoUninitialize();
    }
    return ret;
}

gint32 DestroyContext()
{
    IService* pSvc = g_pIoMgr;
    if( pSvc != nullptr )
    {
        pSvc->Stop();
        g_pIoMgr.Clear();
    }

    CoUninitialize();
    DebugPrintEx( logErr, 0,
        "#Leaked objects is %d",
        CObjBase::GetActCount() );
    return STATUS_SUCCESS;
}


FactoryPtr InitClassFactory()
{
    BEGIN_FACTORY_MAPS;

    INIT_MAP_ENTRYCFG( CAsyncTest_CliImpl );
    
    
    END_FACTORY_MAPS;
}

extern "C"
gint32 DllLoadFactory( FactoryPtr& pFactory )
{
    pFactory = InitClassFactory();
    if( pFactory.IsEmpty() )
        return -EFAULT;
    return STATUS_SUCCESS;
}

gint32 maincli(
    CAsyncTest_CliImpl* pIf,
    int argc, char** argv );

int main( int argc, char** argv)
{
    gint32 ret = 0;
    do{
        ret = InitContext();
        if( ERROR( ret ) )
            break;
        
        InterfPtr pIf;
        CParamList oParams;
        oParams[ propIoMgr ] = g_pIoMgr;
        ret = CRpcServices::LoadObjDesc(
            "./asynctstdesc.json",
            "AsyncTest",
            false, oParams.GetCfg() );
        if( ERROR( ret ) )
            break;
        
        ret = pIf.NewObj(
            clsid( CAsyncTest_CliImpl ),
            oParams.GetCfg() );
        if( ERROR( ret ) )
            break;
        
        CAsyncTest_CliImpl* pSvc = pIf;
        ret = pSvc->Start();
        if( ERROR( ret ) )
            break;
        
        while( pSvc->GetState()== stateRecovery )
            sleep( 1 );
        
        if( pSvc->GetState() != stateConnected )
        {
            ret = ERROR_STATE;
            break;
        }

        maincli( pSvc, argc, argv );
        
        // Stopping the object
        ret = pSvc->Stop();
        
    }while( 0 );

    DestroyContext();
    return ret;
}

//-----Your code begins here---
gint32 maincli(
    CAsyncTest_CliImpl* pIf,
    int argc, char** argv )
{
    //-----Adding your code here---
    gint32 ret = 0;
    do{
        stdstr i0, i0r;
        i0 = "hello, LongWait";
        ret = pIf->LongWait( i0, i0r );
        if( ERROR( ret ) )
        {
            OutputMsg( ret,
                "LongWait failed with error " );
            break;
        }
        OutputMsg( ret,
            "LongWait completed with response %s",
            i0r.c_str() );
        
        ret = pIf->LongWaitNoParam( nullptr );
        if( ERROR( ret ) )
        {
            OutputMsg( ret,
                "LongWaitNoParam failed with error " );
            break;
        }
        if( ret == STATUS_PENDING )
        {
            pIf->WaitForComplete();
            ret = pIf->GetError();
            if( ERROR( ret ) )
            {
                OutputMsg( ret,
                    "LongWaitNoParam failed with error " );
                break;
            }
        }
        OutputMsg( ret, "LongWaitNoParam completed" );

        i0 = "hello, LongWait2";
        ret = pIf->LongWait2( i0, i0r );
        if( ERROR( ret ) )
        {
            OutputMsg( ret, "LongWait2 failed with error " );
            break;
        }
        OutputMsg( ret,
            "LongWait2 completed with response %s",
            i0r.c_str() );

    }while( 0 );
    return ret;
}

