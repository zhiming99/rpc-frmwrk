all:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sip-build --verbose

install:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sudo sip-install --verbose

dist:
distdir:

.PHONY: install
