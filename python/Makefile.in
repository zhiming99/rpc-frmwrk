all:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sudo sip-install --verbose --debug --tracing

debug:

build:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sip-build --verbose

install:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sudo sip-install --verbose

release:
	@if [ ! -e pyproject.toml ]; then bash gentoml.sh;fi
	sudo sip-install --verbose

.PHONY: build debug release
